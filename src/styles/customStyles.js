export const customStylesCategories = {
  control: () => ({
    height: 40,
    borderRadius: 0,
    fontSize: 12,
    border: '1px solid #dbdde2',
    display: 'flex',
    alignItems: 'center',
  }),
  indicatorsContainer: () => null,
};

export const customStylesProvisionNotice = {
  control: () => ({
    height: 40,
    borderRadius: 0,
    fontSize: 12,
    border: '1px solid #dbdde2',
    display: 'flex',
    alignItems: 'center',
  }),
  container: base => ({
    ...base,
    width: '100%',
  }),
  indicatorsContainer: () => null,
};

export const customStylesSelectSeller = {
  control: () => ({
    height: 40,
    borderRadius: 0,
    fontSize: 12,
    border: '1px solid #dbdde2',
    display: 'flex',
    alignItems: 'center',
  }),
  container: base => ({
    ...base,
    width: '100%',
  }),
  indicatorsContainer: () => null,
};

export const customStylesProductTags = {
  control: () => ({
    height: 40,
    borderRadius: 0,
    fontSize: 13,
    border: '1px solid #dbdde2',
    display: 'flex',
    alignItems: 'center',
  }),
  container: base => ({
    ...base,
    width: '600px',
  }),
};
